plugin.caddy {
  db {
    table = tx_quickshop_products
    sku   = sku
    min   = quantity_min
    max   = quantity_max
  }
  getpost {
    uid = tx_browser_pi1|showUid
    qty = tx_quickshop_qty
  }
}